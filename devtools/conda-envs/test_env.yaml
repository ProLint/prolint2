name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Package dependencies
  - mdanalysis  
  - bottle
  - requests
  - networkx

    # Pip-only installs
  - pip:
      - logomaker
    #  - pmda
  #  - codecov

